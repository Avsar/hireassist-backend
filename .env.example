# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:8000,https://cubea.nl

# SQLite database path
DB_PATH=companies.db

# Optional: only required for agent_discover.py --use-ai-cleanup
ANTHROPIC_API_KEY=
